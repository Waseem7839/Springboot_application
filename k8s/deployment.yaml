apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-app
  namespace: waseem-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: springboot-app
  template:
    metadata:
      labels:
        app: springboot-app
    spec:
      containers:
      - name: springboot-app
        image: waseem951/nginx-docker:latest
        ports:
        - containerPort: 8080
